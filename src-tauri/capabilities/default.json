{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the LiveCap overlay window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
